# Docker

This folder contains Docker-based development files.

| File | Purpose |
| --- | --- |
| `Dockerfile` | Python 3.12 development image |
| `docker-compose.yml` | Local build, run, test, and check command surface |

Run commands from the repository root with:

```powershell
docker-compose -f 30_Docker/docker-compose.yml <command>
```
